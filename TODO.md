- [ ] Update `app.js` to mount `routes/adminroutes.js`
- [ ] Implement `routes/adminroutes.js` with:
  - [ ] GET `/addemployee` (isLogedIn + isAdmin) -> renders `views/addemployee.ejs`
  - [ ] POST `/api/admin/addemployee` (isLogedIn + isAdmin) -> uses `adminController.addEmployee`
- [ ] Implement `controller/admincontroller.js`:
  - [ ] Create `User` (role: 'employee') from submitted credentials
  - [ ] Create `Employee` document linked to the created User
  - [ ] Return JSON success/error
- [ ] Update `views/adminDashboard.ejs` to include a link to `/addemployee`
- [ ] Fill `views/addemployee.ejs` with a simple form that submits via `fetch` (POST) and then redirects to `/adminDashboard`
- [ ] Quick manual test flow:
  - [ ] Login as admin
  - [ ] Open `/adminDashboard` -> click Add Employee
  - [ ] Submit form -> verify user + employee created
